// TYPES
API_TYPE(PUBLIC, DebugState,
  int enabled;
)

API_TYPE(PRIVATE, DebugState,
  int enabled;
)

// FUNCTIONS
API_FN(PUBLIC,  void, fw_init, (void))
API_FN(PRIVATE, void, fw_internal_step, (int ticks))

